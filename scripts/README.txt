This folder (scripts) contains numerous Linux scripts, each of
which is designed to analyze an entire dataset at once. These
must be specially written for each experiment, and must be run
on a Linux machine with the `fish` terminal installed.

-Jordan Dehmel